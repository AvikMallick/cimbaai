export class SummaryResponse {
	constructor(id, url, username, content, timestamp) {
		this.id = id;
		this.url = url;
		this.username = username;
		this.content = content;
		this.timestamp = timestamp;
	}
}
